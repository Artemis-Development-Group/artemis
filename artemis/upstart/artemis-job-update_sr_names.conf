description "update the branch name search cache"

manual
task
stop on artemis-stop or runlevel [016]

nice 10

script
    . /etc/default/artemis
    wrap-job paster run $ARTEMIS_INI -c 'from r2.lib import branch_search; branch_search.load_all_branches()'
end script
