description "update the rising pages"

task
manual
stop on artemis-stop or runlevel [016]

nice 10

script
    . /etc/default/artemis
    wrap-job paster run $ARTEMIS_INI -c 'from r2.lib import rising; rising.set_rising()'
end script
