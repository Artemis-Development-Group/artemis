description "remove expired tokens from hardcache"

manual
task
stop on artemis-stop or runlevel [016]

nice 10

script
    . /etc/default/artemis
    wrap-job paster run $ARTEMIS_INI -c 'from r2.lib.hardcachebackend import delete_expired; delete_expired()'
end script
