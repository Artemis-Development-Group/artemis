description "Update the popular branches."

manual
task
stop on artemis-stop or runlevel [016]

nice 10

script
    . /etc/default/artemis
    wrap-job paster run $ARTEMIS_INI -c 'from r2.models.branch import Branch; Branch.update_popular_branches()'
end script
