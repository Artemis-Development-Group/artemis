description "pulls promotion stats from traffic db and writes them to cassandra"

manual
task
stop on artemis-stop or runlevel [016]

nice 10

script
    . /etc/default/artemis
    wrap-job paster run $ARTEMIS_INI -c 'from r2.lib import inventory; inventory.update_prediction_data()'
end script
