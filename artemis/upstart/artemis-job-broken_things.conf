description "find and delete new broken things (usually caused by failed transactions)"

manual
task
stop on artemis-stop or runlevel [016]

nice 10

script
    . /etc/default/artemis
    wrap-job paster run $ARTEMIS_INI -c 'from r2.lib.utils import utils; utils.find_recent_broken_things(delete=True)'
end script
