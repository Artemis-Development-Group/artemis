description "process hourly traffic logs on EMR"

instance $slice

manual
task
stop on artemis-stop or runlevel [016]

nice 10

script
    . /etc/default/artemis
    wrap-job paster run $ARTEMIS_INI -c "from r2.lib.traffic import process_hour; process_hour(\"$slice\")"
end script
